type Query {
  userById(id: String!): User!
  me: User!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}

input CreateUserInput {
  username: String!
  email: String!
  password: String!
}

type User {
  id: String!
  username: String!
}
